spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ragchatbot?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
# Cấu hình Qdrant
qdrant:
  host: localhost
  port: 6334                    # Dùng cổng gRPC
  http-port: 6333               # Dùng cổng HTTP cho REST API
  collection-name: documents
  vector-size: 3072             # gemini-embedding-001 = 3072 dimensions

# Cấu hình Gemini
gemini:
  api-key: AIzaSyBoE7yf4FxTPIokqZsoXzIPBCeJjBSDdrQ
  embedding-model: gemini-embedding-001
  chat-model: gemini-2.0-flash

# Thư mục lưu file upload
app:
  upload-dir: ./uploads

server:
  port: 8080





